# HTTP: ACME challenges + redirect to HTTPS (used after TLS bootstrap).
server {
    listen 80;
    server_name search-demo.odis.org;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://search.odis.org$request_uri;
    }
}

server {
    listen 80 default_server;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
